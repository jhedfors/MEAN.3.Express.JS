<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="/css/style.css" media="screen" title="no title" charset="utf-8">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
<script type ="text/javascript" src="/socket.io/socket.io.js"></script>
<script type ="text/javascript">
    $(document).ready(function (){
        // this triggers the connection event in our server!
        var socket = io.connect();
        // we'll write all the socket stuff after the above line!
        $('button').click(function (){
            console.log("VALUE",$('#myForm').val());
            var data = {name: $('#name').val(),location: $('#location').val(),language: $('#language').val(),comment: $('#comment').val() }
            socket.emit("button_clicked", data);
        });
        socket.on('server_response', function (data){
            console.log('The server says: ' + {info: req.body});
        });
    })
</script>
  </head>
  <body>
  <form id="myForm" action="/result" method="post">
    <label for="">Your Name:</label><input type="text" name="name" id="name">
    <label for="">Dojo Location:</label>
    <select class="" name="location" id="name">
      <option>Seattle</option>
      <option>Mountain View</option>
      <option>San Jose</option>
    </select>
    <label for="">Favorite Language:</label>
    <select class="" name="language" id="name">
      <option>Javascript</option>
      <option>Python</option>
      <option>MEAN</option>
    </select>
    <textarea name="comment" rows="8" cols="40" id="name"></textarea>
    <button type="button" name="button">Submit</button>
    <!-- <input type="submit" value="Submit"> -->
  </form>
  </body>
</html>
